# New ERP — มาตรฐานเอกสาร PDF

อัปเดตล่าสุด: 15 กันยายน 2026

ใช้มาตรฐานนี้ทุกครั้งที่สร้างหรือแก้ไข PDF ในทุก module ของ `new-erp` เป้าหมายคือ PDF ภาษาไทยที่อ่านง่าย พิมพ์ A4 ได้สม่ำเสมอ ตรวจสอบย้อนหลังได้ และมีข้อมูลตามกฎหมายเมื่อเอกสารอยู่ในขอบเขตของกรมสรรพากร

รายการหน้า/เมนูที่ควรมี PDF สถานะปัจจุบัน และลำดับพัฒนาอยู่ที่ [`PDF_COVERAGE_CHECKLIST.md`](PDF_COVERAGE_CHECKLIST.md)

> มาตรฐานหน้าตาเพียงอย่างเดียวไม่รับประกันว่าเอกสารได้รับการยอมรับทางภาษี ต้องเลือกประเภทเอกสารให้ถูกต้อง มีข้อมูลจริงครบถ้วน และให้ Accounting/Tax owner ตรวจรับก่อนเปิดใช้จริง ห้ามเรียกเอกสารว่าใบกำกับภาษีหรือ e-Tax Invoice หากยังไม่ผ่านเงื่อนไขของเอกสารประเภทนั้น

## 1. เลือกประเภทเอกสารก่อนเริ่ม

ทุก PDF ต้องระบุประเภทหนึ่งประเภทใดอย่างชัดเจน:

- `INTERNAL` — เอกสารภายใน เช่น ใบเบิก ใบรับสินค้า รายงาน Stock
- `COMMERCIAL` — เอกสารทางการค้า เช่น ใบเสนอราคา ใบสั่งซื้อ ใบแจ้งหนี้ที่ไม่ใช่ใบกำกับภาษี
- `RECEIPT` — ใบเสร็จรับเงิน/ใบรับ
- `TAX_INVOICE_FULL` — ใบกำกับภาษีเต็มรูป
- `TAX_INVOICE_ABBREVIATED` — ใบกำกับภาษีอย่างย่อ
- `DEBIT_NOTE` / `CREDIT_NOTE` — ใบเพิ่มหนี้/ใบลดหนี้
- `WITHHOLDING_CERTIFICATE` — หนังสือรับรองการหักภาษี ณ ที่จ่าย
- `E_TAX` / `E_RECEIPT` — เอกสารอิเล็กทรอนิกส์ที่เข้าสู่กระบวนการ e-Tax ของกรมสรรพากรจริง

เอกสารภาษี ใบรับ หนังสือรับรอง และแบบราชการต้องมี layout และ validation เฉพาะ ห้ามนำ layout เอกสารทั่วไปมาเปลี่ยนเพียงชื่อหัวกระดาษ

## 2. สถาปัตยกรรมที่ต้องใช้

- สร้าง PDF ผ่าน `App\Modules\Platform\Services\DocumentPdfRenderer` และ `renderView()` เท่านั้น ห้ามสร้าง PDF renderer ซ้ำในแต่ละ module
- A4 ใช้ profile `a4` จาก `config/erp.php`: A4 แนวตั้ง, UTF-8, `Noto Sans Thai`, margin บน/ล่าง 16 mm และซ้าย/ขวา 12 mm
- ใช้ Blade PDF แยกจากหน้าจอ ห้ามส่ง HTML หน้าจอหรือ Bootstrap ทั้งหน้าเข้า mPDF
- PDF route เป็น read-only: การเปิด พิมพ์ หรือดาวน์โหลดต้องไม่เปลี่ยน status, Stock, GL หรือ audit state ของเอกสาร
- Controller ต้องตรวจ `auth`, permission สำหรับพิมพ์, company/branch/warehouse scope และสิทธิ์เข้าถึงเอกสารก่อน render
- Query เอกสารและ relation ที่ใช้ให้เสร็จก่อนเข้า view ห้าม query ฐานข้อมูลจาก Blade
- ห้ามโหลดรูปจาก URL ภายนอก ใช้ local path ที่ตรวจสอบแล้วเท่านั้น เพื่อป้องกัน SSRF และปัญหา render ไม่คงที่
- Blade ต้อง escape ข้อมูลผู้ใช้ตามปกติ ห้ามใช้ `{!! !!}` กับชื่อ ที่อยู่ หมายเหตุ หรือข้อความจากเอกสาร
- Renderer ปัจจุบันตัด `<style>` จาก view ก่อนส่งให้ mPDF ดังนั้น view ใหม่ต้องใช้ class/CSS ที่ renderer กลางรองรับ หรือ inline style ที่อนุมัติและทดสอบกับ mPDF อย่าพึ่ง browser-only CSS

## 3. Payload กลาง

Controller/presenter ของแต่ละเอกสารควรส่งข้อมูลในรูปแบบนี้ โดยเพิ่ม field เฉพาะประเภทได้เท่าที่จำเป็น:

```php
[
    'company' => [
        'name', 'address', 'tax_id', 'branch_code', 'branch_label',
        'logo_path',
    ],
    'document' => [
        'type', 'title', 'number', 'issue_date', 'due_date',
        'status', 'currency', 'copy_label',
    ],
    'party' => [
        'code', 'name', 'address', 'tax_id', 'branch_code', 'branch_label',
    ],
    'references' => [
        ['label', 'number', 'date'],
    ],
    'lines' => [
        ['line_number', 'item', 'description', 'uom', 'quantity',
         'unit_price', 'discount', 'tax_base', 'tax_rate', 'tax_amount', 'amount'],
    ],
    'totals' => [
        'subtotal', 'discount', 'tax_base', 'vat', 'withholding',
        'rounding', 'grand_total', 'amount_in_words',
    ],
    'notes' => [],
    'signatures' => [
        ['role', 'name', 'position', 'signed_at', 'image_path'],
    ],
]
```

กติกาข้อมูล:

- ใช้ Decimal/formatter กลางของ domain ห้ามคำนวณเงินด้วย `float` ใน PDF
- จำนวนเงิน รายละเอียด VAT และยอดรวมใน PDF ต้องตรงกับ document snapshot และ Journal ที่ post แล้ว
- เอกสาร DRAFT อาจอ่าน Company Settings ปัจจุบันได้ แต่เมื่อออก/อนุมัติ/post เอกสารที่มีผลทางกฎหมาย ต้อง snapshot ชื่อ ที่อยู่ เลขภาษี สาขา คู่ค้า อัตราภาษี ผู้ลงนาม และ reference ที่จำเป็น เพื่อให้พิมพ์ย้อนหลังแล้วได้ข้อมูลเดิม
- Logo เริ่มต้นมาจาก `CompanySetting.logo_path`; สำหรับเอกสารที่ออกแล้วให้ใช้ logo version/path ที่ snapshot ไว้ถ้ามี
- ค่าที่ไม่มีจริงให้แสดง `—` เฉพาะ field ที่ไม่บังคับ ห้ามใช้ `—` แทนข้อมูลบังคับของเอกสารภาษี

## 4. มาตรฐานหน้าตา A4

ลำดับ layout:

1. Header: logo ซ้าย ข้อมูลบริษัทกลาง/ซ้าย ชื่อเอกสารและคำว่า `ต้นฉบับ`/`สำเนา` ทางขวา
2. Document metadata: เลขที่ วันที่ออก สถานะ สกุลเงิน และเลขอ้างอิง
3. Company/branch และคู่ค้า พร้อมชื่อ ที่อยู่ เลขภาษี และสำนักงานใหญ่/สาขาเมื่อเกี่ยวข้อง
4. ตารางรายการ
5. ส่วนลด ฐานภาษี VAT ภาษีหัก ณ ที่จ่าย การปัดเศษ ยอดสุทธิ และจำนวนเงินตัวอักษร
6. หมายเหตุและเงื่อนไข
7. ผู้จัดทำ ผู้ตรวจสอบ ผู้อนุมัติ/ผู้รับเงิน เฉพาะบทบาทที่เอกสารต้องใช้
8. Footer: เลขที่เอกสาร เลขหน้า `หน้า {PAGENO} / {nbpg}` และข้อความควบคุมเอกสาร

รูปแบบกลาง:

- Font: `Noto Sans Thai`; body 9.5–10.5 pt, heading 12–14 pt, document title 16–18 pt และห้ามต่ำกว่า 8.5 pt
- Logo: รักษาอัตราส่วน สูงไม่เกิน 22 mm กว้างไม่เกิน 45 mm และมีข้อความชื่อบริษัทแม้ไม่มี logo
- ใช้สีดำ/เทาเข้มเป็นหลัก พื้นหัวตารางสีอ่อน เส้นตารางชัด และต้องอ่านได้เมื่อพิมพ์ขาวดำ
- ตัวเลขและจำนวนเงินชิดขวา วันที่/เลขเอกสารไม่ตัดบรรทัด รายละเอียดข้อความยาวตัดบรรทัดได้
- `thead` ต้องซ้ำทุกหน้า, ห้ามตัดหนึ่งแถวข้ามหน้า และหน้าสุดท้ายต้องแสดงยอดรวมครบถ้วน
- Header/footer ที่เป็นข้อมูลควบคุมเอกสารต้องซ้ำทุกหน้า เอกสารหลายหน้าต้องบอกเลขหน้าและสถานะหน้าต่อไป
- Signature block อยู่ท้ายหน้าสุดท้ายเสมอ เมื่อรายการน้อยให้ดันลงใกล้ขอบล่างของพื้นที่พิมพ์ เมื่อรายการมากให้ขึ้นหน้าใหม่ทั้ง block แทนการตัดครึ่ง
- ห้ามใส่ประวัติระบบ, internal database ID, debug text หรือ URL ภายในลงในเอกสารที่ส่งให้คู่ค้า เว้นแต่เป็นเอกสาร audit โดยตั้งใจ

## 5. Logo บริษัท

- อ่านจาก `GlobalSettings::value('logo_path')`/`CompanySetting.logo_path`
- ตรวจ `Storage::disk('public')->exists($path)` ก่อนแปลงเป็น local absolute path ให้ mPDF
- รองรับ PNG/JPEG พื้นโปร่งหรือพื้นขาว; ห้าม remote URL และ SVG ที่ผู้ใช้อัปโหลด
- ถ้าไฟล์หายหรือเสีย PDF ต้องยังสร้างได้โดยแสดงชื่อบริษัทแทน และบันทึกปัญหาให้ผู้ดูแลตรวจสอบ
- ห้าม hard-code logo ใน module หรือ Blade

## 6. ภาพลายเซ็นจาก User Profile

ใช้ภาพลายเซ็นเฉพาะเมื่อ workflow และประเภทเอกสารกำหนดบทบาทผู้ลงนามจริง เช่น ผู้อนุมัติ ผู้รับเงิน หรือผู้มีอำนาจลงนาม

- Source กลางต้องมาจาก User Profile เช่น `users.signature_path`; ห้ามเพิ่ม field ลายเซ็นแยกตาม module
- Source จาก User Profile ใช้สร้างหลักฐาน ณ เวลาเกิด action เท่านั้น; PDF ต้องอ่านจาก `document_signature_snapshots` ผ่าน `<x-platform::pdf-signatures>` และห้ามอ่านภาพปัจจุบันย้อนหลัง
- รับเฉพาะ PNG/JPEG ไม่เกิน 2 MB ตรวจ MIME จริง จำกัดมิติ และเก็บใน private/protected storage; แนะนำ PNG พื้นโปร่ง
- ตอน approve/post ให้ snapshot `user_id`, ชื่อ, ตำแหน่ง, วันเวลา, signature path/version และ hash ของไฟล์ ห้ามพิมพ์ย้อนหลังจากรูปลายเซ็นล่าสุดใน Profile
- แสดงภาพเฉพาะเอกสารสถานะที่ลงนามแล้วและ signer ตรงกับบทบาท ห้ามแสดงบน DRAFT โดยอัตโนมัติ
- ถ้าเอกสารอนุญาตให้ลงนามภายหลังและไม่มีรูป ให้แสดงเส้นลงนาม ชื่อ ตำแหน่ง และวันที่ ห้ามนำลายเซ็นของผู้ใช้อื่นมา fallback
- การใช้ เปลี่ยน หรือลบภาพลายเซ็นต้องมี audit log และต้องไม่แก้ snapshot ของเอกสารเดิม
- ไฟล์ลายเซ็นที่ถูก snapshot แล้วห้ามลบ object เดิมเมื่อผู้ใช้เปลี่ยนหรือลบลายเซ็นใน Profile
- ภาพลายเซ็นเป็นเพียงภาพที่มองเห็น ไม่ใช่ลายมือชื่อดิจิทัลและไม่ใช่หลักฐานว่า PDF เป็น e-Tax/e-Receipt

## 7. เอกสารที่กรมสรรพากรตรวจสอบ

### ใบกำกับภาษีเต็มรูป

ตามมาตรา 86/4 ต้องมีอย่างน้อย:

1. คำว่า `ใบกำกับภาษี` เด่นชัด
2. ชื่อ ที่อยู่ และเลขประจำตัวผู้เสียภาษีของผู้ออก
3. ชื่อและที่อยู่ผู้ซื้อ/ผู้รับบริการ
4. หมายเลขลำดับเอกสารและเล่ม (ถ้ามี)
5. ชื่อ ชนิด ประเภท ปริมาณ และมูลค่าสินค้าหรือบริการ
6. จำนวน VAT แยกจากมูลค่าสินค้าหรือบริการอย่างชัดเจน
7. วัน เดือน ปี ที่ออก
8. ข้อความอื่นตามที่อธิบดีกำหนด

ในทางปฏิบัติต้องแสดงเลขภาษีและสำนักงานใหญ่/สาขาของสถานประกอบการที่ออกเอกสาร รวมถึงข้อมูลผู้ซื้อตามเงื่อนไขที่ใช้กับเอกสารนั้น ตรวจว่าเลขภาษี 13 หลักและรหัสสาขา 5 หลักถูกต้องก่อนออกเอกสาร ใช้ภาษาไทย หน่วยเงินบาท และตัวเลขไทยหรืออารบิก เว้นแต่ได้รับอนุมัติเป็นอย่างอื่น

### เอกสารหลายหน้า

- ทุกหน้าต้องระบุตัวตนเอกสารและมีเลขหน้า
- ใบกำกับภาษีหลายแผ่นต้องทำตามคำสั่ง ป.86/2542: แต่ละหน้ามีรายการสำคัญที่กฎหมายกำหนด แสดงว่ามีหน้าต่อไป และยอดรวม/ยอด VAT ทั้งฉบับอยู่หน้าสุดท้าย
- ห้ามทำให้ยอด subtotal, VAT หรือ grand total ซ้ำจนตีความว่าเป็นคนละเอกสาร

### ใบเพิ่มหนี้/ใบลดหนี้

นอกจากข้อมูลผู้ออก ผู้ซื้อ เลขที่ และวันที่ ต้องอ้างอิงใบกำกับภาษีเดิม แสดงมูลค่าเดิม มูลค่าที่ถูกต้อง ผลต่าง VAT และเหตุผลสั้น ๆ ที่ออกเอกสาร ห้ามใช้ใบลดหนี้เป็นเพียง PDF ยกเลิกรายการโดยไม่มี source linkage

### ใบรับและหนังสือรับรองหัก ณ ที่จ่าย

ใช้ layout และ field ตามประเภท/แบบที่กฎหมายกำหนด พร้อมสำเนาและ retention ที่เกี่ยวข้อง ต้องผ่าน Accounting/Tax owner sign-off แยกจากใบกำกับภาษี

### e-Tax Invoice / e-Receipt

PDF ปกติหรือ PDF ที่วางภาพลายเซ็นยังไม่ใช่ e-Tax Invoice/e-Receipt การเปิดใช้ `E_TAX` ต้องมีขั้นตอนขึ้นทะเบียน รูปแบบข้อมูลอิเล็กทรอนิกส์ การลงลายมือชื่อดิจิทัลด้วยใบรับรองที่ยอมรับ การนำส่ง และการเก็บรักษาตามระบบกรมสรรพากร ห้ามติดป้าย e-Tax จน integration และ compliance test ผ่าน

แหล่งอ้างอิงทางการ:

- [ประมวลรัษฎากร มาตรา 85–86 รวมมาตรา 86/4, 86/9 และ 86/10](https://www.rd.go.th/5208.html)
- [คำสั่งกรมสรรพากร ป.86/2542 เรื่องการจัดทำใบกำกับภาษี](https://www.rd.go.th/3568.html)
- [ประกาศอธิบดีฯ VAT ฉบับที่ 39 เรื่องสำนักงานใหญ่/สาขา](https://www.rd.go.th/3400.html)
- [ระเบียบกรมสรรพากรเกี่ยวกับใบกำกับภาษีและใบรับอิเล็กทรอนิกส์](https://www.rd.go.th/31224.html)
- [คู่มือกรมสรรพากร: การสร้างลายมือชื่อดิจิทัลสำหรับ e-Tax/e-Receipt](https://etax.rd.go.th/etax_staticpage/app/emag/flipbook_2567/pdf/06_Digital-Signature.pdf)

## 8. การควบคุมเอกสารและความปลอดภัย

- เอกสาร DRAFT ต้องมี watermark `ร่าง` และห้ามแสดงเป็นต้นฉบับทางภาษี
- เอกสาร VOID/CANCELLED ต้องมี watermark `ยกเลิก` แต่ยังเก็บเลขที่และ audit linkage เดิม
- เอกสารที่ออกแล้วห้ามแก้ข้อมูลย้อนหลัง ให้ใช้เอกสารแก้ไข/ยกเลิกตาม workflow
- การพิมพ์ซ้ำต้องแสดง `พิมพ์ซ้ำ` และวันเวลาพิมพ์เมื่อ policy ของเอกสารกำหนด โดยไม่เปลี่ยนเลขที่เอกสาร
- ชื่อไฟล์ใช้เลขเอกสารที่ sanitize แล้ว เช่น `IVHQ2609000001.pdf`; response ต้องเป็น `application/pdf` และใช้ `inline` สำหรับดู หรือ `attachment` สำหรับดาวน์โหลดตาม action
- เก็บ audit ของการออก/อนุมัติ/post/ยกเลิก ไม่จำเป็นต้องสร้าง audit ใหม่ทุกครั้งที่เปิด PDF เว้นแต่เป็นข้อกำหนดของเอกสารสำคัญ

## 9. Checklist ก่อนส่งมอบ

### ข้อมูลและกฎหมาย

- [ ] ระบุ document classification แล้ว และชื่อเอกสารตรงกับผลทางกฎหมายจริง
- [ ] ข้อมูลบริษัท สาขา คู่ค้า เลขภาษี วันที่ เลขที่ และ references ครบ
- [ ] จำนวน รายการ ส่วนลด ฐานภาษี VAT/WHT และยอดสุทธิตรงกับ source snapshot/GL
- [ ] เอกสารภาษีผ่าน checklist เฉพาะประเภทและ Accounting/Tax owner sign-off
- [ ] ไม่มีการเรียก PDF ปกติว่า e-Tax/e-Receipt

### Layout และไฟล์

- [ ] ภาษาไทยแสดงครบด้วย Noto Sans Thai ไม่มีสี่เหลี่ยมหรือตัวอักษรซ้อน
- [ ] Logo จาก Company Settings แสดงถูกสัดส่วน และกรณีไม่มีไฟล์ยัง render ได้
- [ ] A4, margin, ตาราง, header/footer, page number และลำดับข้อมูลตรงมาตรฐาน
- [ ] ทดสอบ 0, 1, 30 และอย่างน้อย 100 lines; header ซ้ำและ totals/signatures อยู่หน้าสุดท้าย
- [ ] ทดสอบชื่อ/ที่อยู่/รายละเอียดภาษาไทยยาว ตัวเลขติดลบ ทศนิยม และ page break
- [ ] พิมพ์ขาวดำแล้วยังอ่านสถานะ เส้นตาราง และยอดรวมได้ชัด

### ลายเซ็น ความปลอดภัย และสิทธิ์

- [ ] เอกสารที่ต้องลงนามใช้ signer ตาม workflow และ snapshot ลายเซ็นตอนลงนาม
- [ ] ไม่มีลายเซ็นของผู้ใช้ผิดคน ไม่มี remote image และไฟล์ผ่าน MIME/size validation
- [ ] Route ตรวจ auth, print permission และ company/branch/warehouse scope
- [ ] PDF render เป็น read-only และข้อมูลผู้ใช้ทั้งหมดถูก escape

### Automated checks

- [ ] Unit/feature test ยืนยัน `Content-Type: application/pdf` และไฟล์ขึ้นต้น `%PDF-`
- [ ] HTML contract test ตรวจหัวเอกสาร เลขที่ วันที่ ยอดรวม ข้อมูลภาษี และ signer ที่บังคับ
- [ ] Multipage smoke test ตรวจ page break, repeated header, page number และ signature block
- [ ] ทดสอบ missing/corrupt logo และ missing signature โดย PDF ยังเปิดได้
- [ ] `php artisan view:cache` และ `git diff --check` ผ่าน

## 10. สถานะ foundation ปัจจุบัน

พร้อมใช้แล้ว:

- mPDF shared renderer
- A4 profile และ `Noto Sans Thai`
- Company Settings: ชื่อ ที่อยู่ เลขภาษี และ `logo_path`

ยังต้องทำก่อนประกาศรองรับครบ:

- User Profile ยังไม่มีภาพลายเซ็น/ตำแหน่งงานและ signature snapshot contract
- Branch ยังไม่มีที่อยู่ เลขภาษี และรหัสสาขาสำหรับเอกสารภาษีโดยตรง
- PDF เดิมแต่ละ module ยังต้อง audit เทียบมาตรฐานนี้ โดยเฉพาะเอกสารภาษีและ multipage
- e-Tax/e-Receipt integration และ digital signature ยังไม่ใช่ feature ที่เปิดใช้งาน
