> **หมายเหตุ:** เนื้อหาด้านล่างเป็นการวิเคราะห์รุ่นแรก แนวคิด `ขายจากสต็อก` เทียบ `ผลิตตามคำสั่งขาย` ในฐานะนโยบายบังคับถูกแทนที่ด้วย **สินค้าทุกชิ้นขายผ่านสต็อก + flag `สั่งผลิตได้` ที่อนุญาตให้ผู้ใช้สร้าง WO เมื่อสมัครใจ** และอนุญาตขายจากสต็อกแม้ WO ยังไม่เสร็จ ดูข้อตกลงล่าสุดและคำถามที่ยังเปิดใน [POS_PRODUCTION_INTEGRATION_CHECKLIST.md](POS_PRODUCTION_INTEGRATION_CHECKLIST.md)

ได้ครับ รอบนี้เป็น **การวิเคราะห์และออกแบบเท่านั้น ยังไม่แก้โค้ด** ประเด็นสำคัญคือ **“สินค้านี้ผลิตได้” ไม่เท่ากับ “ใบสั่งขายรายการนี้ต้องสั่งผลิต”** และ POS ต้องทำงานได้ตามปกติเมื่อไม่ได้เปิด Production

## โครงสร้างที่มีอยู่แล้ว

เส้นทางปัจจุบันคือ:

`ใบสั่งขาย (POS) → ยืนยัน → รายการคำสั่งขายรอผลิต (Production) → สร้าง WO แบบ Make to Order → รับสินค้าผลิตเสร็จเข้าสต็อก → สร้าง/ลงรายการขาย HS/IV`

ระบบเชื่อมเอกสารระดับ **Sales Order line** อยู่แล้ว ไม่ใช่เพียงระดับหัวเอกสาร:

- Production ดึงรายการจากใบสั่งขายสถานะ `CONFIRMED` ที่มี Active BOM ในสาขา และยังไม่มี WO ที่ไม่ถูกยกเลิก (`OrderController.php:423–455`)
- การสร้าง WO ตรวจสาขา สินค้าสต็อก หน่วยสินค้า และ BOM ก่อนสร้าง; WO ผูก `sales_order_id` และ `sales_order_line_id` โดยจำนวนผลิตปัจจุบันเท่ากับ **เต็มจำนวนของรายการขาย** (`ProductionOrderService.php:151–239`)
- เมื่อผลิตครบและรับสินค้าเข้าสต็อก ระบบจองสินค้าสำเร็จรูปให้ Sales Order line นั้น; ตอนลงรายการขายจะใช้ยอดจองดังกล่าว (`ManualProductionReceiptPostingService.php:274–314`, `PhysicalSalePostingService.php:109–120`)
- Production เปิดใช้ได้เฉพาะโปรไฟล์ `MANUFACTURING` ที่ตั้ง `production_enabled`; เส้นทาง Production มี capability และ permission กำกับ (`ModuleCapability.php:25–35`, `Production/Routes/web.php:8–18`)

ดังนั้น **ไม่ต้องออกแบบการเชื่อม WO–SO ใหม่ทั้งหมด**; ส่วนที่ขาดหลัก ๆ คือ *กติกาว่ารายการไหนควรผลิต* และ *การสื่อสารกติกานั้นใน POS*

## ช่องว่างที่พบ

1. **ข้อมูลสินค้าไม่มีนโยบายการจัดหา** ขณะนี้ WMS Item มี `GOODS/SERVICE` และ `is_stock_item` แต่ไม่มีค่าอย่าง “ขายจากสต็อก / ผลิตตามคำสั่งขาย” (`Item.php:16–20`, `items/form.blade.php:10–12`) ส่วน Active BOM บอกเพียงว่า *ผลิตสินค้านี้ได้* ไม่ได้บอกว่า *ทุกออเดอร์ต้องผลิต*
2. **POS แสดงปุ่มสร้าง WO กว้างเกินจริง** ในหน้ารายละเอียดใบสั่งขาย รายการที่ยืนยันแล้วและผู้ใช้มีสิทธิ์สร้าง WO จะเห็นปุ่ม แม้รายการนั้นไม่มี BOM, หน่วยไม่ตรง, เป็นสินค้าที่ขายจากสต็อก หรือไม่ได้เปิด Production; ผู้ใช้จะรู้ปัญหาหลังคลิกแล้วเท่านั้น (`sales-orders/show.blade.php:14`)
3. **สถานะการจัดหามองไม่เห็นก่อนยืนยัน** หน้านั้นแสดง WO เมื่อสร้างแล้ว แต่ยังไม่บอกว่าแต่ละรายการ “ขายจากสต็อก”, “ควรวางแผนผลิต”, “รอ BOM” หรือ “กำลังผลิต” และคำแนะนำหลักพาไปสร้าง HS/IV ทันทีหลังยืนยัน แม้รายการอาจยังรอผลิต
4. **POS ยังอ้าง Production โดยตรง** หน้าแสดงใบสั่งขาย query ตาราง WO โดยไม่ตรวจ capability ก่อน (`SalesOrderController.php:113–124`) นี่เป็นจุดที่ควรจัดระเบียบสำหรับโมดูล optional รวมถึงการซ่อนลิงก์/ปุ่มที่ผู้ใช้ไม่มีสิทธิ์เข้าถึง
5. **จำนวนและการส่งมอบบางส่วนยังเป็นข้อจำกัด** การสร้าง WO จาก SO ปัจจุบันวางแผนเต็มจำนวนหนึ่งรายการ ไม่ใช่คำนวณเฉพาะส่วนที่ขาด; การจองสินค้าสำเร็จรูปเกิดเมื่อผลิตครบตาม WO ไม่ใช่เพียงรับบางส่วน จึงยังไม่ควรแสดงคำว่า “พร้อมส่งมอบ” จากสถานะ WO อย่างเดียว

## แบบที่เสนอ

ให้แยกข้อมูลออกเป็น **2 ชั้น**:

| ชั้น | คำถามที่ตอบ | ข้อเสนอ |
|---|---|---|
| **นโยบายสินค้า** | ปกติสินค้านี้จัดหามาอย่างไร? | ค่าเช่น `ขายจากสต็อก` / `ผลิตตามคำสั่งขาย` เฉพาะเมื่อเปิด Production; ถ้าต่างกันตามสาขาจริง ค่อยกำหนดระดับสาขา |
| **ความพร้อมของ SO line** | ออเดอร์นี้ ณ ตอนนี้ต้องทำอะไรต่อ? | คำนวณจากนโยบาย + Active BOM ของสาขา + หน่วย + WO ที่ผูกอยู่ + สต็อก/ยอดจองที่เกี่ยวข้อง |

**อย่าใช้ “มี BOM” เป็นเครื่องหมายว่าต้องสร้าง WO**: สินค้าผลิตเก็บสต็อกก็มี BOM ได้ และสินค้า Make to Order อาจยังตั้ง BOM ไม่ครบ ซึ่งควรแสดงว่า **“ตั้งค่า BOM ก่อน”** ไม่ใช่หายไปจากรายการรอผลิต

### UX ที่ใบสั่งขาย

- **ขณะดูรายการก่อนยืนยัน:** แสดงข้อความสั้น ๆ รายบรรทัด เช่น `ขายจากสต็อก`, `ผลิตตามคำสั่งขาย · พร้อมสร้าง WO`, `ผลิตตามคำสั่งขาย · ยังไม่มี Active BOM` เพื่อให้ฝ่ายขายทราบก่อนรับปากวันส่ง โดยไม่สร้าง WO อัตโนมัติ
- **หลังยืนยัน:** ในคอลัมน์ “การจัดหา/Production” แสดง **สถานะและขั้นถัดไป** ไม่ใช่ปุ่มสร้าง WO ทุกบรรทัด ตัวอย่าง `รอสร้าง WO` → `WO ร่าง` → `กำลังผลิต` → `ผลิตครบ/รับเข้าคลังแล้ว`; กรณีผิดเงื่อนไขให้บอกเหตุผลและลิงก์ไปแก้ข้อมูลตามสิทธิ์
- **ส่วนสรุปด้านบน:** ถ้ามีรายการรอผลิต แสดง “ใบสั่งขายนี้มี X รายการรอวางแผนผลิต” พร้อมคำอธิบายว่าการยืนยัน SO **ไม่ใช่การสั่งผลิตอัตโนมัติ**; ไม่แย่งความเด่นจาก action หลัก
- **เมื่อ Production ปิด:** POS ไม่แสดงสถานะหรือปุ่ม WO และยังขายสินค้า/ตัดสต็อกตามกระบวนการ POS ปกติ ข้อมูล WO เดิมและยอดจองเดิมต้องยังรักษาความถูกต้อง ไม่ลบทิ้งเพราะปิดโมดูล

## ลำดับงานที่แนะนำเมื่อเริ่ม implement

1. **ตกลงกติกาธุรกิจก่อน:** Make to Order ใช้กับสินค้าระดับบริษัทหรือแยกสาขา? ถ้ามีสต็อกพร้อมอยู่แล้ว ยังต้องผลิตใหม่หรืออนุญาตใช้สต็อก? รับ/ส่งมอบบางส่วนได้หรือไม่? ฝ่ายขายยืนยัน SO ที่ BOM ไม่พร้อมได้หรือไม่?
2. ปรับการจำแนกสินค้าที่ต้นทาง พร้อมกำหนดค่าเริ่มต้นที่ไม่เปลี่ยนพฤติกรรมสินค้าที่มีอยู่
3. ใช้กติกาเดียวกันคำนวณสถานะใน SO และรายการ “คำสั่งขายรอผลิต” เพื่อตัดปัญหาหนึ่งหน้าบอกให้ผลิต แต่อีกหน้าหาไม่เจอ
4. ปรับ POS ให้ตรวจ **capability + permission + ความพร้อมจริง** ก่อนแสดง action; คงการตรวจซ้ำฝั่ง server และ idempotency ที่มีอยู่
5. ค่อยพิจารณาเรื่อง WO เฉพาะยอดขาดและการรับ/ส่งมอบบางส่วน **หลังตกลงนโยบาย** ไม่รวมไว้ในงาน UX รอบแรก

**ข้อเสนอสำหรับ MVP:** เริ่มจาก “ระบุนโยบายสินค้า + แสดงคำแนะนำ/สถานะที่ถูกต้อง + สร้าง WO ด้วยคนกด” ก่อน ไม่สร้าง WO อัตโนมัติและไม่บล็อกการยืนยันใบสั่งขายเพียงเพราะ BOM ยังไม่พร้อม แต่ต้องแจ้งข้อจำกัดให้เห็นชัดเจน

🌱 graft saved ~2,621,919 tokens this turn

## ข้อสรุปหลังวิเคราะห์

ผู้ใช้เลือก **1A / 2A / 3A / 4A** ในคำถามชุดแรก ต่อมาปรับแนวคิดเป็น flag `สั่งผลิตได้`: ขายจากสต็อกเสมอ, สร้าง WO เต็ม SO line ได้แม้มีสต็อก, และขายจากสต็อกได้แม้ WO ยังไม่เสร็จ คำถามชุดที่สอง **1A / 2A / 3A / 4A** (SO เก่า, flag ล่าสุด, HS/IV ที่มีอยู่, คลังต่างกัน) ได้รับคำตอบแล้ว ดูข้อตกลงล่าสุดและรายการตรวจสอบที่ [POS_PRODUCTION_INTEGRATION_CHECKLIST.md](POS_PRODUCTION_INTEGRATION_CHECKLIST.md)
