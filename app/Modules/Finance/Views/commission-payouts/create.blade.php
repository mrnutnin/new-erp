@extends('Finance::layout')
@section('title', 'สร้างเอกสารจ่ายคอมมิชชั่น | Finance')
@section('content')
<div class="container-fluid px-3 px-lg-4 py-4"><p class="eyebrow mb-2">FINANCE / COMMISSION PAYOUTS</p><h1 class="h3 mb-2">สร้างเอกสารจ่ายคอมมิชชั่น</h1><p class="text-secondary mb-4">ชุด {{ $commissionPaymentBatch->document_number }} · ผู้รับ {{ $recipient->name }}</p><form id="payout-form" action="{{ route('finance.commission-payouts.payouts.store', $commissionPaymentBatch) }}"><div class="card border-0 shadow-sm"><div class="card-body p-3 p-lg-4"><input type="hidden" name="recipient_user_id" value="{{ $recipient->id }}"><div class="row g-3"><div class="col-md-4"><label class="form-label">วันที่จ่าย <span class="text-danger">*</span></label><input class="form-control" type="date" name="document_date" value="{{ now()->format('Y-m-d') }}" required></div><div class="col-md-8"><label class="form-label">บัญชีเงินสด/ธนาคาร <span class="text-danger">*</span></label><select class="form-select" name="bank_account_id" required><option value="">เลือกบัญชี</option>@foreach($banks as $bank)<option value="{{ $bank->id }}">{{ $bank->code }} · {{ $bank->name }}</option>@endforeach</select></div></div><div class="mt-4 d-flex gap-2"><a class="btn btn-outline-secondary" href="{{ route('finance.commission-payouts.show', $commissionPaymentBatch) }}">ย้อนกลับ</a><button class="btn btn-dark" type="submit">สร้างเอกสารจ่าย</button></div></div></div></form></div>
@endsection
@push('scripts')<script>$(function(){$('#payout-form').on('submit',function(e){e.preventDefault();const f=$(this),b=f.find(':submit').prop('disabled',true);$.ajax({url:f.attr('action'),method:'POST',data:f.serialize(),headers:{'X-CSRF-TOKEN':$('meta[name="csrf-token"]').attr('content'),Accept:'application/json'}}).done(r=>location.href=r.redirect).fail(x=>Swal.fire({icon:'error',text:x.responseJSON?.message||'ไม่สามารถสร้างเอกสารจ่ายได้'})).always(()=>b.prop('disabled',false));});});</script>@endpush
