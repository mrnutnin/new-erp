@extends('Finance::layout')
@section('title', ($advance->exists ? 'แก้ไข' : 'สร้าง').'ใบเงินทดรองจ่าย | Finance')
@section('content')
<div class="container-fluid px-3 px-lg-4 py-4"><div class="d-flex justify-content-between align-items-end gap-3 mb-4"><div><p class="eyebrow mb-2">FINANCE / EMPLOYEE ADVANCE</p><h1 class="h3 mb-1">{{ $advance->exists ? 'แก้ไขใบเงินทดรองจ่าย' : 'สร้างใบเงินทดรองจ่าย' }}</h1><p class="text-secondary mb-0">บันทึกได้เฉพาะ Draft และระบบกำหนดเลขเอกสารอัตโนมัติ</p></div><a class="btn btn-outline-secondary" href="{{ $advance->exists ? route('finance.employee-advances.show',$advance) : route('finance.employee-advances.index') }}">กลับ</a></div><form id="employee-advance-form" method="POST" action="{{ $advance->exists ? route('finance.employee-advances.update',$advance) : route('finance.employee-advances.store') }}">@csrf @if($advance->exists)@method('PUT')@endif<div class="card border-0 shadow-sm"><div class="card-body p-3 p-lg-4"><div class="row g-3"><div class="col-md-6"><label class="form-label">ผู้รับเงิน / Employee <span class="text-danger">*</span></label><select class="form-select" name="employee_user_id" required><option value="">เลือก User</option>@foreach($userOptions as $id=>$label)<option value="{{ $id }}" @selected((int)old('employee_user_id',$advance->employee_user_id)===(int)$id)>{{ $label }}</option>@endforeach</select></div><div class="col-md-6"><label class="form-label">บัญชีจ่าย <span class="text-danger">*</span></label><select class="form-select" name="bank_account_id" required><option value="">เลือกบัญชี</option>@foreach($bankAccountOptions as $id=>$label)<option value="{{ $id }}" @selected((int)old('bank_account_id',$advance->bank_account_id)===(int)$id)>{{ $label }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">วันที่เอกสาร <span class="text-danger">*</span></label><input class="form-control" type="date" name="document_date" value="{{ old('document_date',optional($advance->document_date)->format('Y-m-d')) }}" required></div><div class="col-md-4"><label class="form-label">วันที่ต้องคืน</label><input class="form-control" type="date" name="due_date" value="{{ old('due_date',optional($advance->due_date)->format('Y-m-d')) }}"></div><div class="col-md-4"><label class="form-label">จำนวนเงิน <span class="text-danger">*</span></label><input class="form-control text-end" type="number" name="amount" min="0.01" step="0.01" value="{{ old('amount',$advance->amount) }}" required></div><div class="col-12"><label class="form-label">วัตถุประสงค์ <span class="text-danger">*</span></label><textarea class="form-control" name="purpose" rows="3" maxlength="1000" required>{{ old('purpose',$advance->purpose) }}</textarea></div></div></div></div><div class="mt-4 d-flex gap-2"><button class="btn btn-dark" type="submit">{{ $advance->exists ? 'บันทึกการแก้ไข' : 'บันทึกฉบับร่าง' }}</button><a class="btn btn-outline-secondary" href="{{ route('finance.employee-advances.index') }}">ยกเลิก</a></div></form></div>
@endsection
@push('scripts')<script>$(function(){window.erpAjaxForm({form:'#employee-advance-form',redirect:true});});</script>@endpush
