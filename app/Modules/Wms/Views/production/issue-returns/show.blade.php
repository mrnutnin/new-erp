@extends('Wms::layout')
@php($statusLabels=['DRAFT'=>'ร่าง','APPROVED'=>'อนุมัติแล้ว','POSTED'=>'ลง Stock แล้ว','VOID'=>'ยกเลิก','REVERSED'=>'กลับรายการแล้ว'])
@php($statusClasses=['DRAFT'=>'neutral','APPROVED'=>'info','POSTED'=>'success','VOID'=>'danger','REVERSED'=>'warning'])
@section('title','รับคืนวัตถุดิบผลิต '.$document->document_number.' | WMS')
@section('content')<div class="container-fluid px-3 px-lg-4 py-4"><div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-end gap-3 mb-4"><div><p class="eyebrow mb-2">WMS / PRODUCTION ISSUE RETURN</p><h1 class="h3 mb-2">{{ $document->document_number }}</h1><span class="badge app-status-{{ $statusClasses[$document->status] ?? 'neutral' }}">{{ $statusLabels[$document->status] ?? $document->status }}</span></div><div class="d-flex flex-wrap gap-2"><a class="btn btn-outline-secondary" href="{{ route('wms.production.issue-returns.index') }}">← กลับ</a>@if($document->status==='DRAFT')<button class="btn btn-dark js-production-return-action" data-url="{{ route('wms.production.issue-returns.approve',$document) }}" data-action="approve">อนุมัติ</button>@endif @if($document->status==='APPROVED')<button class="btn btn-dark js-production-return-action" data-url="{{ route('wms.production.issue-returns.post',$document) }}" data-action="post">ลง Stock</button>@endif @if(in_array($document->status,['DRAFT','APPROVED'],true))<button class="btn btn-app-danger js-production-return-action" data-url="{{ route('wms.production.issue-returns.cancel',$document) }}" data-action="cancel">ยกเลิกรายการรับคืน</button>@endif @if($document->status==='POSTED')<button class="btn btn-app-danger js-production-return-action" data-url="{{ route('wms.production.issue-returns.reverse',$document) }}" data-action="reverse">กลับรายการ</button>@endif @if($document->status==='DRAFT')<button class="btn btn-app-danger js-production-return-action" data-url="{{ route('wms.production.issue-returns.destroy',$document) }}" data-action="delete">ลบร่าง</button>@endif</div></div><div class="card border-0 shadow-sm mb-4"><div class="card-body p-3 p-lg-4"><div class="row g-3"><div class="col-md-3"><small class="text-secondary">วันที่เอกสาร</small><div class="fw-semibold">{{ $document->document_date?->format($dateFormat) ?: '-' }}</div></div><div class="col-md-3"><small class="text-secondary">ใบเบิกต้นทาง</small><div class="fw-semibold"><a class="link-primary" href="{{ route('wms.production.material-issues.show',$document->issue) }}">{{ $document->issue?->document_number }} <i class="bx bx-link-external"></i></a></div></div><div class="col-md-3"><small class="text-secondary">ประเภท</small><div class="fw-semibold">เบิกวัตถุดิบผลิต</div></div><div class="col-md-3"><small class="text-secondary">คลัง</small><div class="fw-semibold">{{ $document->warehouse?->code }} · {{ $document->warehouse?->name }}</div></div><div class="col-12"><small class="text-secondary">เหตุผล</small><div class="fw-semibold">{{ $document->reason ?: '-' }}</div></div></div></div></div><div class="card border-primary-subtle shadow-sm mb-4"><div class="card-body p-3 p-lg-4"><h2 class="h5 mb-3">รายละเอียดใบเบิกต้นทาง</h2><div class="table-responsive"><table class="table table-sm align-middle mb-0"><thead><tr><th>#</th><th>สินค้า</th><th>หน่วย</th><th class="text-end">จำนวนเบิก</th></tr></thead><tbody>@foreach($document->issue?->lines ?? [] as $line)<tr><td>{{ $line->line_number }}</td><td>{{ $line->item?->code }} · {{ $line->item?->name }}</td><td>{{ $line->uom?->code }}</td><td class="text-end">{{ AppModulesWmsSupportWmsDecimal::format($line->quantity) }}</td></tr>@endforeach</tbody></table></div></div></div><div class="card border-0 shadow-sm mb-4"><div class="card-body p-3 p-lg-4"><h2 class="h5 mb-3">รายการรับคืนวัตถุดิบ</h2><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>#</th><th>สินค้า</th><th>หน่วย</th><th class="text-end">จำนวน</th><th>Movement</th><th class="text-end">ต้นทุน</th></tr></thead><tbody>@forelse($document->lines as $line)<tr><td>{{ $line->line_number }}</td><td>{{ $line->issueLine?->item?->code }} · {{ $line->issueLine?->item?->name }}</td><td>{{ $line->issueLine?->uom?->code }}</td><td class="text-end">{{ AppModulesWmsSupportWmsDecimal::format($line->quantity) }}</td><td>{{ $line->movement?->id ? '#'.$line->movement->id : '-' }}</td><td class="text-end">{{ $line->allocation ? AppModulesWmsSupportWmsDecimal::format($line->allocation->value) : '-' }}</td></tr>@empty<tr><td colspan="6" class="text-center text-secondary">ไม่พบรายการรับคืน</td></tr>@endforelse</tbody></table></div></div></div><div class="card border-0 shadow-sm"><div class="card-body p-3 p-lg-4"><h2 class="h5 mb-3">ประวัติเอกสาร</h2>@forelse($history as $entry)<div class="border-bottom py-2"><strong>{{ $entry->action }}</strong><small class="text-secondary ms-2">{{ $entry->created_at?->format($dateFormat.' H:i') }} · {{ $entry->user?->name ?? '-' }}</small></div>@empty<div class="text-secondary">ยังไม่มีประวัติ</div>@endforelse</div></div></div>@endsection
@push('scripts')<script>$(function(){$('.js-production-return-action').on('click',function(){const b=$(this),a=b.data('action');Swal.fire({icon:'warning',title:a==='approve'?'อนุมัติใบรับคืนวัตถุดิบ?':a==='post'?'ลง Stock ใบรับคืนวัตถุดิบ?':a==='reverse'?'กลับรายการใบรับคืน?':a==='cancel'?'ยกเลิกรายการรับคืน?':'ลบร่างใบรับคืน?',input:['cancel','reverse'].includes(a)?'textarea':undefined,inputLabel:a==='reverse'?'เหตุผลการกลับรายการ':a==='cancel'?'เหตุผลการยกเลิก':undefined,showCancelButton:true,confirmButtonText:'ยืนยัน',cancelButtonText:'ยกเลิก',confirmButtonColor:['cancel','reverse','delete'].includes(a)?'#dc3545':undefined}).then(x=>{if(!x.isConfirmed)return;const data={_token:$('meta[name=csrf-token]').attr('content')};if(['cancel','reverse'].includes(a))data.reason=x.value;$.ajax({url:b.data('url'),type:a==='delete'?'DELETE':'POST',data}).done(r=>{Swal.fire({icon:'success',text:r.msg,timer:1100,showConfirmButton:false}).then(()=>location.href=r.redirect||location.href)}).fail(x=>Swal.fire({icon:'error',text:x.responseJSON?.message||'ดำเนินการไม่สำเร็จ'}));});});});</script>@endpush
