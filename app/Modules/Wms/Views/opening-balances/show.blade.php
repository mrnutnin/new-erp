@extends('Wms::layout')
@section('title', 'ยอดยกมาสินค้า')
@section('content')
<div class="container-fluid px-3 px-lg-4 py-4"><div class="d-flex flex-wrap justify-content-between align-items-start gap-3 mb-4"><div><p class="eyebrow mb-2">WMS / OPENING BALANCE</p><h1 class="h3 mb-2">ยอดยกมาสินค้า #{{ $batch->id }}</h1><p class="text-secondary mb-0">{{ $batch->cutover_date?->format('d/m/Y') }} · {{ $batch->costing_method }} · {{ $batch->warehouse?->code }}</p></div><div class="d-flex gap-2"><a class="btn btn-outline-secondary" href="{{ route('wms.opening-balances.index') }}">กลับรายการ</a>@if($batch->status==='DRAFT' && auth()->user()->hasPermission('wms.opening-balances.post'))<button class="btn btn-dark" id="post-opening">ลงบัญชี</button>@endif</div></div><div class="card border-0 shadow-sm"><div class="card-body p-4"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>สินค้า</th><th>หน่วย</th><th class="text-end">จำนวน</th><th class="text-end">ต้นทุน/หน่วย</th><th class="text-end">รวม</th></tr></thead><tbody>@foreach($batch->lines as $line)<tr><td>{{ $line->item?->code }} · {{ $line->item?->name }}</td><td>{{ $line->uom?->code }}</td><td class="text-end">{{ number_format((float)$line->quantity, 8) }}</td><td class="text-end">{{ number_format((float)$line->unit_cost, 2) }}</td><td class="text-end">{{ number_format((float)$line->total_value, 2) }}</td></tr>@endforeach</tbody><tfoot><tr><th colspan="4" class="text-end">รวม</th><th class="text-end">{{ number_format((float)$batch->total_value, 2) }}</th></tr></tfoot></table></div></div></div></div>
@endsection
@push('scripts')
@if($batch->status==='DRAFT')<script>$('#post-opening').on('click',function(){var b=$(this).prop('disabled',true);$.ajax({url:'{{ route('wms.opening-balances.post',$batch) }}',method:'POST',headers:{'X-CSRF-TOKEN':'{{ csrf_token() }}',Accept:'application/json'}}).done(function(r){window.location=r.redirect;}).fail(function(x){Swal.fire({icon:'error',text:x.responseJSON?.message||'ลงบัญชีไม่สำเร็จ'});b.prop('disabled',false);});});</script>@endif
@endpush
