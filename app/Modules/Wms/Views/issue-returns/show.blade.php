@extends('Wms::layout')
@section('title','ใบรับคืน '.$document->document_number.' | WMS')
@section('content')<div class="container-fluid px-3 px-lg-4 py-4"><div class="d-flex justify-content-between align-items-end mb-4"><div><p class="eyebrow mb-2">WMS / ISSUE RETURN</p><h1 class="h3 mb-2">{{ $document->document_number }}</h1><span class="badge app-status-{{ $document->status==='POSTED'?'success':($document->status==='APPROVED'?'info':'neutral') }}">{{ ['DRAFT'=>'ร่าง','APPROVED'=>'อนุมัติแล้ว','POSTED'=>'ลง Stock แล้ว','VOID'=>'ยกเลิก'][$document->status] ?? $document->status }}</span></div><div class="d-flex gap-2"><a class="btn btn-outline-secondary" href="{{ route('wms.issue-returns.index') }}">← กลับ</a>@if($document->status==='DRAFT' && auth()->user()->hasPermission('wms.issue-returns.approve'))<button class="btn btn-app-soft js-act" data-action="approve">อนุมัติ</button>@endif @if($document->status==='APPROVED' && auth()->user()->hasPermission('wms.issue-returns.post'))<button class="btn btn-app-soft js-act" data-action="post">ลง Stock</button>@endif</div></div><div class="card border-0 shadow-sm mb-4"><div class="card-body"><div class="row g-3"><div class="col-md-3"><small class="text-secondary">วันที่เอกสาร</small><div>{{ $document->document_date?->format($dateFormat) }}</div></div><div class="col-md-3"><small class="text-secondary">ใบเบิกต้นทาง</small><div>{{ $document->issue?->document_number ?: '-' }}</div></div><div class="col-md-6"><small class="text-secondary">เหตุผล</small><div>{{ $document->reason }}</div></div></div></div></div><div class="card border-0 shadow-sm mb-4"><div class="card-body"><h2 class="h5 mb-3">รายการรับคืน</h2><div class="table-responsive"><table class="table align-middle"><thead><tr><th>#</th><th>สินค้า</th><th>หน่วย</th><th class="text-end">จำนวน</th><th>Movement</th><th>ต้นทุน</th></tr></thead><tbody>@foreach($document->lines as $line)<tr><td>{{ $line->line_number }}</td><td>{{ $line->issueLine?->item?->code }} · {{ $line->issueLine?->item?->name }}</td><td>{{ $line->issueLine?->uom?->code }}</td><td class="text-end">{{ \App\Modules\Wms\Support\WmsDecimal::format($line->quantity) }}</td><td>{{ $line->movement?->id ? '#'.$line->movement->id : '-' }}</td><td class="text-end">{{ $line->allocation ? \App\Modules\Wms\Support\WmsDecimal::format($line->allocation->value) : '-' }}</td></tr>@endforeach</tbody></table></div></div></div><div class="card border-0 shadow-sm"><div class="card-body"><h2 class="h5 mb-3">ประวัติเอกสาร</h2>@forelse($history as $h)<div class="border-bottom py-2"><strong>{{ $h->action }}</strong><small class="text-secondary ms-2">{{ $h->created_at?->format($dateFormat.' H:i') }} · {{ $h->user?->name ?? '-' }}</small></div>@empty<div class="text-secondary">ยังไม่มีประวัติ</div>@endforelse</div></div></div>@endsection
@push('scripts')<script>$(function(){$('.js-act').on('click',function(){const a=$(this).data('action');Swal.fire({icon:'warning',title:a==='approve'?'อนุมัติใบรับคืน?':'ลง Stock ใบรับคืน?',showCancelButton:true,confirmButtonText:'ยืนยัน',cancelButtonText:'ยกเลิก'}).then(x=>{if(!x.isConfirmed)return;$.post('{{ route('wms.issue-returns.show',$document) }}/'+a,{_token:$('meta[name=csrf-token]').attr('content')}).done(()=>location.reload()).fail(e=>Swal.fire({icon:'error',text:e.responseJSON?.message||'ดำเนินการไม่สำเร็จ'}));});});});</script>@endpush
