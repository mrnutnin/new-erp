@extends('Crm::layout')
@section('title','ทีมขาย | CRM')
@section('content')
<div class="container-fluid px-3 px-lg-4 py-4 crm-page module-dashboard module-dashboard--crm"><div class="module-dashboard-hero d-flex flex-column flex-md-row justify-content-between align-items-md-end gap-3 mb-4 crm-page-header"><div><p class="module-dashboard-kicker mb-2"><span></span>CRM / SALES TEAM</p><h1>ทีมขาย</h1><p class="mb-0">กำหนดหัวหน้าทีมและสมาชิกสำหรับควบคุมขอบเขตงานของทีม</p></div><a class="btn btn-app-primary" href="{{ route('crm.teams.create') }}"><i class="bx bx-plus me-1"></i>สร้างทีมขาย</a></div><div class="row g-3">@forelse($teams as $team)<div class="col-12 col-lg-6"><article class="card h-100"><div class="card-body"><div class="d-flex justify-content-between gap-2"><div><h2 class="h5 mb-1">{{ $team->name }}</h2><div class="text-secondary"><i class="bx bx-user-check me-1"></i>{{ $team->manager?->name }}</div></div><span class="badge {{ $team->is_active?'app-status-success':'app-status-neutral' }}">{{ $team->is_active?'ใช้งาน':'ปิดใช้งาน' }}</span></div><div class="mt-3 small text-secondary">สมาชิก {{ number_format($team->members_count) }} คน</div><div class="d-flex flex-wrap gap-1 mt-2">@foreach($team->members->take(8) as $member)<span class="badge app-status-info">{{ $member->name }}</span>@endforeach @if($team->members_count>8)<span class="badge app-status-neutral">+{{ $team->members_count-8 }}</span>@endif</div><div class="text-end mt-3"><a class="btn btn-sm btn-app-soft" href="{{ route('crm.teams.edit',$team) }}"><i class="bx bx-edit me-1"></i>แก้ไข</a></div></div></article></div>@empty<div class="col-12"><div class="card"><div class="card-body text-center py-5 text-secondary">ยังไม่มีทีมขายในสาขานี้</div></div></div>@endforelse</div></div>
@endsection
