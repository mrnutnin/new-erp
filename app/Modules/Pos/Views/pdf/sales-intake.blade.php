@include('Pos::pdf.partials.modern-style')
@if($logo)<img class="logo" src="{{ $logo }}">@endif
<h1>{{ $companyName }}</h1><div class="muted">{{ $companyAddress ?: '—' }}</div><h2>ใบรับข้อมูลเบื้องต้น</h2>
<table class="doc-meta"><tbody><tr><td width="34%">เลขที่<br><b>{{ $intake->document_number }}</b></td><td width="33%">วันที่เอกสาร<br><b>{{ $intake->document_date?->format($dateFormat) ?: '—' }}</b></td><td>สถานะ<br><b>{{ ['DRAFT'=>'ร่าง','COMPLETED'=>'เสร็จสิ้น','CANCELLED'=>'ยกเลิก'][$intake->status] ?? $intake->status }}</b></td></tr></tbody></table>
@include('Pos::pdf.partials.document-detail-header', ['document' => $intake])
<h3>รายการสินค้า / บริการ</h3><table><thead><tr><th>#</th><th>รายการ</th><th>หน่วย</th><th class="right">จำนวน</th><th class="right">ราคา/หน่วย</th><th class="right">ส่วนลด</th><th class="right">รวม</th></tr></thead><tbody>@forelse($intake->lines as $line)<tr><td>{{ $line->line_number }}</td><td>{{ data_get($line->item_snapshot,'code','—') }} · {{ data_get($line->item_snapshot,'name',$line->description) }}@if($line->description)<br><span class="muted">{{ $line->description }}</span>@endif</td><td>{{ $line->uom?->code ?: data_get($line->uom_snapshot,'code','—') }}</td><td class="right">{{ number_format((float)$line->quantity,$decimalPlaces) }}</td><td class="right">{{ number_format((float)($line->requested_unit_price ?? $line->standard_unit_price ?? 0),2) }}</td><td class="right">{{ number_format((float)$line->discount_amount,2) }}</td><td class="right">{{ number_format((float)$line->line_total,2) }}</td></tr>@empty<tr><td colspan="7">ไม่มีรายการ</td></tr>@endforelse<tr class="total"><td colspan="6" class="right">รวมสุทธิ</td><td class="right">{{ number_format((float)$intake->grand_total,2) }}</td></tr></tbody></table>
@if($intake->description)<h3>หมายเหตุ</h3><div>{{ $intake->description }}</div>@endif
<div class="footer">จัดทำโดย {{ $intake->preparedBy?->name ?: '—' }} · พิมพ์เมื่อ {{ now()->format($dateFormat.' H:i') }}</div>
