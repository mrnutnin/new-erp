@php($status = ['POSTED' => 'ลงบัญชีแล้ว', 'PARTIAL' => 'ใช้บางส่วน', 'APPLIED' => 'ใช้ครบแล้ว', 'REFUNDED' => 'คืนเงินแล้ว'][$advanceDeposit->status] ?? $advanceDeposit->status)
<htmlpageheader name="advance-deposit-reference"><div style="font-family:notosansthai;font-size:8.5pt;color:#737b83;text-align:right">{{ $companyName }} · {{ $advanceDeposit->document_number }} · {{ optional($advanceDeposit->document_date)->format($dateFormat) }}</div></htmlpageheader>
<sethtmlpageheader name="advance-deposit-reference" value="on" show-this-page="1" />
<div class="pdf-tax-invoice"><table class="pdf-header"><tr>@if($logo)<td class="invoice-logo" width="16%"><img src="{{ $logo }}" style="width:24mm;max-height:22mm"></td>@endif<td class="pdf-header-company" width="{{ $logo ? '44%' : '60%' }}"><h1>{{ $companyName ?: 'บริษัท' }}</h1><div>{{ $companyAddress ?: '—' }}</div>@if($companyTaxId)<div class="muted">เลขประจำตัวผู้เสียภาษี {{ $companyTaxId }}</div>@endif</td><td class="pdf-header-title" width="40%"><div class="pdf-copy">เอกสารทางการเงิน</div><h2>ใบรับเงินล่วงหน้า</h2><div class="invoice-number">{{ $advanceDeposit->document_number }}</div><div class="muted">สถานะ: {{ $status }}</div></td></tr></table>
<table class="pdf-party"><tr><td width="60%" class="invoice-buyer"><div class="pdf-label">ลูกค้า</div><div class="pdf-value">{{ $advanceDeposit->party?->code ?: '—' }} · {{ $advanceDeposit->party?->name ?: '—' }}</div><div>{{ $advanceDeposit->party?->address ?: '—' }}</div></td><td width="40%" class="invoice-details"><table class="invoice-meta"><tr><td>วันที่เอกสาร</td><td class="right">{{ $advanceDeposit->document_date?->format($dateFormat) ?: '—' }}</td></tr><tr><td>ภาษี</td><td class="right">{{ $advanceDeposit->prices_include_vat ? 'ราคารวม VAT' : 'ราคาไม่รวม VAT' }}</td></tr></table></td></tr></table>
<table class="pdf-product"><thead><tr><th width="10%">#</th><th>บัญชีเงินสด/ธนาคาร</th><th width="25%">เลขอ้างอิง</th><th width="22%" class="right">จำนวนเงิน</th></tr></thead><tbody>@forelse($advanceDeposit->tenders as $tender)<tr><td class="center">{{ $loop->iteration }}</td><td>{{ $tender->bankAccount?->code ?: '—' }} · {{ $tender->bankAccount?->name ?: '—' }}</td><td>{{ $tender->reference ?: '—' }}</td><td class="right">{{ number_format((float) $tender->amount, 2) }}</td></tr>@empty<tr><td colspan="4" class="center">ไม่มีข้อมูลช่องทางรับเงิน</td></tr>@endforelse</tbody></table></div>
<!-- invoice-closing --><div class="pdf-tax-invoice"><table class="pdf-footer"><tr><td width="55%" class="pdf-footer-payment"><div class="pdf-footer-heading">รายละเอียดเพิ่มเติม</div><p class="muted">เอกสารนี้ไม่ใช่ใบกำกับภาษี</p></td><td width="45%" class="pdf-footer-total"><table class="pdf-total-summary"><tr><td>ยอดรับ</td><td class="right">{{ number_format((float) $advanceDeposit->original_amount, 2) }}</td></tr><tr><td>หัก ณ ที่จ่าย</td><td class="right">{{ number_format((float) ($advanceDeposit->withholding_amount ?? 0), 2) }}</td></tr><tr class="total"><td>รับสุทธิ</td><td class="right">{{ number_format((float) $advanceDeposit->original_amount - (float) ($advanceDeposit->withholding_amount ?? 0), 2) }}</td></tr></table></td></tr></table><table class="pdf-signatures"><tr><td width="45%"><div class="invoice-sign-space">&nbsp;</div><div>........................................................</div><div>ผู้รับเงิน</div><div class="invoice-sign-date">วันที่ .......... / .......... / ..........</div></td><td width="10%"></td><td width="45%"><div class="invoice-sign-space">&nbsp;</div><div>........................................................</div><div>ผู้อนุมัติ</div><div class="invoice-sign-date">วันที่ .......... / .......... / ..........</div></td></tr></table><p class="pdf-control">{{ $advanceDeposit->document_number }} · ใบรับเงินล่วงหน้า</p></div>
